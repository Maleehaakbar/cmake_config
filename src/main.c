#include <stdio.h>
#include "sum.h"
#include "arr.h"
#include "template_header.h"
int main()
{   
    int s;
    s = sum(3,5);
    printf("sum is %d\n", s);
    array_size();
    printf("project name is %s", project_name);
    return 0;
}